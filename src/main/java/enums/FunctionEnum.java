package enums;

/**
 * @author AAGoncharova
 *
 * This Enum class contains all possible arithmetic functions.
 */
public enum FunctionEnum {
    ADD, //"add"
    MUL, //"mul"
    MUL_TWO_THEN_SUM_WITH_THIRD, //"mul-then-add"
    INCREMENT_THEN_MUL; //"inc-then-mul"
}
